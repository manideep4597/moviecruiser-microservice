management:
    security:
        enabled: false
spring:
    application:
        name: config-server-client
    profiles:
        active: development
    cloud:
        config:
            uri: http://localhost:8092

